persistence:
  enabled: true
  storageClass: local-path
storageClass:
  name: bulk
  mountOptions:
    - vers=3
    - nolock
image:
  repository: ghcr.io/salsadigitalauorg/rockpool/nfs-provisioner
  tag: latest
  pullPolicy: Always
